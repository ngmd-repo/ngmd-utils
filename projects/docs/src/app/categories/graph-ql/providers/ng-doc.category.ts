import { NgDocCategory } from '@ng-doc/core';

import GraphQLCategory from '../ng-doc.category';

const ProvidersCategory: NgDocCategory = {
  title: 'Providers',
  category: GraphQLCategory,
};

export default ProvidersCategory;
