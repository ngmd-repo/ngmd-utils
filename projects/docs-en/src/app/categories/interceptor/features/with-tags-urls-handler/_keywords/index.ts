import { NgDocGlobalKeyword } from '@ng-doc/core';

import { makeModuleKeywords } from '../../../../../../common/handlers';

export const InterceptorWithTagsUrlsHandlerKeywords: Record<string, NgDocGlobalKeyword> =
  makeModuleKeywords('/interceptor/features/with-tags-urls-handler', [
    'withTagsUrlsHandler',
    'InterceptorTagsUrlsHandler',
  ]);
