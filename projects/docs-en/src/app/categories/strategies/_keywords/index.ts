import { NgDocGlobalKeyword } from '@ng-doc/core';

import { TitleStrategyKeywords } from '../provide-title-strategy/_keywords';

export const StrategiesKeywords: Record<string, NgDocGlobalKeyword> = Object.assign(
  {},
  TitleStrategyKeywords,
);
