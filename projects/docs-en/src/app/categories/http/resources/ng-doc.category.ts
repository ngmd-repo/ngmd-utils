import { NgDocCategory } from '@ng-doc/core';

import HttpCategory from '../ng-doc.category';

const ResourcesCategory: NgDocCategory = {
  title: 'Resources',
  category: HttpCategory,
  order: 4,
};

export default ResourcesCategory;
