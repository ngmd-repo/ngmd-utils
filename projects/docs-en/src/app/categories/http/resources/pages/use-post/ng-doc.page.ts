import { NgDocPage } from '@ng-doc/core';

import ResourcesCategory from '../../ng-doc.category';

const UsePostPage: NgDocPage = {
  title: `usePost`,
  mdFile: './index.md',
  category: ResourcesCategory,
  order: 2,
};

export default UsePostPage;
