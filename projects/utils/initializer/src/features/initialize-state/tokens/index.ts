import { InjectionToken } from '@angular/core';

import { InitializeStateOptions } from '../types';

export const INITIALIZE_STATE_OPTIONS = new InjectionToken<InitializeStateOptions>(
  'INITIALIZE_STATE_OPTIONS ',
);
